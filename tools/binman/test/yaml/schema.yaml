---

definitions:
    u8:
        type: integer
        minimum: 0
        maximum: 0xff

type: object
properties:
    main-branch:
        type: object
        properties:
            small-branch:
                type: object
                properties:
                    a:
                        $ref: "#/definitions/u8"
                    b:
                        $ref: "#/definitions/u8"

